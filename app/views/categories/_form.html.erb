<%= form_with(model: @category, url: categories_path, local: true) do |f| %>
  <%= f.label :name, 'カテゴリー名', class: "form-label " %>
  <%= f.text_field :cat_name, class: "form-control form-control-sm" %>
  <%= f.submit "作成", class: "btn btn-outline-secondary btn-sm" %>
<% end %>
<br>


<% @categories.each do |category| %>
  <table>
    <tr>
      <td><%= category.cat_name %></td>
      <td><%= link_to "削除", label_path(label.id), method: :delete, class: 'btn btn-danger btn-sm', data: { confirm: "削除して良いですか？" } %></td>
    </tr>
  </table>
<% end %>
